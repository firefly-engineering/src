# Toolchain declaration for Firefly Engineering Monorepo
# Resolved by turnkey via teller + toolbox registries

[toolchains]
# Build tools
buck2 = {}
tk = {}  # Turnkey CLI wrapper for buck2 with auto-sync

# Language toolchains
go = {}
rust = {}
cargo = {}
python = {}
nodejs = {}
typescript = {}

# Documentation
mdbook = { version = "0.4.52" }  # Pinned for mdbook-admonish compatibility

# Development utilities
nix = {}
jj = {}
beads = {}
beads-viewer = {}
